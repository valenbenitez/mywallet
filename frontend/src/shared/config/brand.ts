/** Product brand shown in UI. Design tokens come from Portrait; brand name does not. */
export const BRAND_NAME = "MyWallet" as const;

export const BRAND_TAGLINE =
  "Your virtual wallet — no keys, no gas complexity." as const;
